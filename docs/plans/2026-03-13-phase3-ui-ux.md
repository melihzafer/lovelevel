# Phase 3: UI/UX Overhaul Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Standardize UI components, improve accessibility, and ensure consistent use of design tokens.

**Architecture:** Component library with variants, sizes, and proper token integration.

**Tech Stack:** React 19, Tailwind CSS v4, Framer Motion, Design Tokens

---

## Overview

This phase addresses UI/UX debt identified in the codebase:
- Inconsistent use of design tokens (hardcoded colors, spacing)
- Missing component variants (sizes, states)
- Incomplete accessibility (focus states, aria labels)
- No component documentation or Storybook

**Estimated Duration:** 5-7 days

**Risk Level:** Medium (UI changes, but backward compatible)

---

## Task 1: Update Tailwind Config to Use Design Tokens

**Goal:** Map Tailwind theme to CSS custom properties from tokens.css.

**Files:**
- Modify: `src/styles/tailwind.css`

**Step 1: Update tailwind.css theme**

Update `src/styles/tailwind.css` to include theme mappings:

```css
@import "tailwindcss";

@theme {
  /* Colors - mapped to CSS custom properties */
  --color-primary-50: var(--color-primary-50);
  --color-primary-100: var(--color-primary-100);
  --color-primary-200: var(--color-primary-200);
  --color-primary-300: var(--color-primary-300);
  --color-primary-400: var(--color-primary-400);
  --color-primary-500: var(--color-primary-500);
  --color-primary-600: var(--color-primary-600);
  --color-primary-700: var(--color-primary-700);
  --color-primary-800: var(--color-primary-800);
  --color-primary-900: var(--color-primary-900);
  
  --color-accent-50: var(--color-accent-50);
  --color-accent-100: var(--color-accent-100);
  --color-accent-200: var(--color-accent-200);
  --color-accent-300: var(--color-accent-300);
  --color-accent-400: var(--color-accent-400);
  --color-accent-500: var(--color-accent-500);
  --color-accent-600: var(--color-accent-600);
  --color-accent-700: var(--color-accent-700);
  --color-accent-800: var(--color-accent-800);
  --color-accent-900: var(--color-accent-900);
  
  /* Semantic colors */
  --color-success: var(--color-success);
  --color-warning: var(--color-warning);
  --color-error: var(--color-error);
  --color-info: var(--color-info);
  
  /* Background */
  --color-bg-primary: var(--bg-primary);
  --color-bg-secondary: var(--bg-secondary);
  --color-bg-tertiary: var(--bg-tertiary);
  
  /* Text */
  --color-text-primary: var(--text-primary);
  --color-text-secondary: var(--text-secondary);
  --color-text-tertiary: var(--text-tertiary);
  --color-text-muted: var(--text-muted);
  
  /* Border */
  --color-border: var(--border-color);
  --color-border-light: var(--border-color-light);
  --color-border-dark: var(--border-color-dark);
  
  /* Spacing */
  --spacing-0: var(--space-0);
  --spacing-1: var(--space-1);
  --spacing-2: var(--space-2);
  --spacing-3: var(--space-3);
  --spacing-4: var(--space-4);
  --spacing-5: var(--space-5);
  --spacing-6: var(--space-6);
  --spacing-8: var(--space-8);
  --spacing-10: var(--space-10);
  --spacing-12: var(--space-12);
  --spacing-16: var(--space-16);
  
  /* Border radius */
  --radius-sm: var(--radius-sm);
  --radius-md: var(--radius-md);
  --radius-lg: var(--radius-lg);
  --radius-xl: var(--radius-xl);
  --radius-2xl: var(--radius-2xl);
  --radius-3xl: var(--radius-3xl);
  --radius-full: var(--radius-full);
  
  /* Shadows */
  --shadow-sm: var(--shadow-sm);
  --shadow-md: var(--shadow-md);
  --shadow-lg: var(--shadow-lg);
  --shadow-xl: var(--shadow-xl);
  --shadow-2xl: var(--shadow-2xl);
  
  /* Transitions */
  --transition-fast: var(--transition-fast);
  --transition-base: var(--transition-base);
  --transition-slow: var(--transition-slow);
  
  /* Z-index */
  --z-dropdown: var(--z-dropdown);
  --z-sticky: var(--z-sticky);
  --z-fixed: var(--z-fixed);
  --z-modal-backdrop: var(--z-modal-backdrop);
  --z-modal: var(--z-modal);
  --z-popover: var(--z-popover);
  --z-tooltip: var(--z-tooltip);
  --z-toast: var(--z-toast);
  
  /* Font family */
  --font-sans: var(--font-family);
  --font-mono: var(--font-mono);
  
  /* Font sizes */
  --font-size-xs: var(--text-xs);
  --font-size-sm: var(--text-sm);
  --font-size-base: var(--text-base);
  --font-size-lg: var(--text-lg);
  --font-size-xl: var(--text-xl);
  --font-size-2xl: var(--text-2xl);
  --font-size-3xl: var(--text-3xl);
  --font-size-4xl: var(--text-4xl);
  --font-size-5xl: var(--text-5xl);
}
```

**Step 2: Verify build**

Run:
```bash
npm run build
```

**Step 3: Commit**

```bash
git add src/styles/tailwind.css
git commit -m "feat: map Tailwind theme to design tokens

- Add @theme directive with CSS custom property mappings
- Enable consistent use of tokens via Tailwind classes
- Support dark mode through token system"
```

---

## Task 2: Enhance Button Component

**Goal:** Add sizes, more variants, and improve accessibility.

**Files:**
- Modify: `src/components/Button.tsx`

**Step 1: Update Button component**

Replace `src/components/Button.tsx`:

```typescript
import type { ButtonHTMLAttributes, ReactNode } from 'react';

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  fullWidth?: boolean;
  loading?: boolean;
  leftIcon?: ReactNode;
  rightIcon?: ReactNode;
}

export function Button({
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  loading = false,
  leftIcon,
  rightIcon,
  className = '',
  children,
  disabled,
  ...props
}: ButtonProps) {
  const baseStyles =
    'inline-flex items-center justify-center font-medium rounded-lg transition-all focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed';

  const sizeStyles = {
    sm: 'min-h-[36px] px-3 py-1.5 text-sm gap-1.5',
    md: 'min-h-[44px] px-4 py-2 text-base gap-2',
    lg: 'min-h-[52px] px-6 py-3 text-lg gap-2.5',
  };

  const variantStyles = {
    primary:
      'bg-primary-500 text-white hover:bg-primary-600 active:bg-primary-700 focus-visible:ring-primary-500',
    secondary:
      'bg-bg-secondary text-text-primary border border-border hover:bg-bg-tertiary active:bg-bg-tertiary/80 focus-visible:ring-primary-500',
    outline:
      'border-2 border-primary-500 text-primary-600 dark:text-primary-400 hover:bg-primary-50 dark:hover:bg-primary-900/30 active:bg-primary-100 dark:active:bg-primary-900/50 focus-visible:ring-primary-500',
    ghost:
      'text-primary-600 dark:text-primary-400 hover:bg-primary-50 dark:hover:bg-primary-900/30 active:bg-primary-100 dark:active:bg-primary-900/50 focus-visible:ring-primary-500',
    danger:
      'bg-error text-white hover:bg-red-600 active:bg-red-700 focus-visible:ring-error',
  };

  return (
    <button
      className={`${baseStyles} ${sizeStyles[size]} ${variantStyles[variant]} ${fullWidth ? 'w-full' : ''} ${className}`}
      disabled={disabled || loading}
      aria-busy={loading}
      {...props}
    >
      {loading ? (
        <svg
          className="animate-spin h-5 w-5"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          aria-hidden="true"
        >
          <circle
            className="opacity-25"
            cx="12"
            cy="12"
            r="10"
            stroke="currentColor"
            strokeWidth="4"
          />
          <path
            className="opacity-75"
            fill="currentColor"
            d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
          />
        </svg>
      ) : (
        <>
          {leftIcon && <span className="flex-shrink-0">{leftIcon}</span>}
          {children}
          {rightIcon && <span className="flex-shrink-0">{rightIcon}</span>}
        </>
      )}
    </button>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Button.tsx
git commit -m "feat: enhance Button component with sizes and variants

- Add sm/md/lg sizes for touch targets
- Add danger variant for destructive actions
- Add loading state with spinner
- Add leftIcon/rightIcon props
- Improve accessibility with aria-busy"
```

---

## Task 3: Enhance Input Component

**Goal:** Add sizes, error states, and improve accessibility.

**Files:**
- Modify: `src/components/Input.tsx`

**Step 1: Update Input component**

Replace `src/components/Input.tsx`:

```typescript
import type { InputHTMLAttributes, ReactNode } from 'react';

interface InputProps extends Omit<InputHTMLAttributes<HTMLInputElement>, 'size'> {
  label?: string;
  error?: string;
  hint?: string;
  size?: 'sm' | 'md' | 'lg';
  leftIcon?: ReactNode;
  rightIcon?: ReactNode;
}

export function Input({
  label,
  error,
  hint,
  size = 'md',
  leftIcon,
  rightIcon,
  className = '',
  id,
  ...props
}: InputProps) {
  const inputId = id || `input-${label?.toLowerCase().replace(/\s+/g, '-')}`;
  const hasError = !!error;

  const containerStyles = 'space-y-1.5';

  const sizeStyles = {
    sm: 'min-h-[36px] px-3 py-1.5 text-sm',
    md: 'min-h-[44px] px-4 py-2 text-base',
    lg: 'min-h-[52px] px-5 py-3 text-lg',
  };

  const inputStyles = `
    w-full rounded-lg border bg-bg-primary text-text-primary
    placeholder:text-text-muted
    focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent
    transition-all
    ${sizeStyles[size]}
    ${hasError ? 'border-error focus:ring-error' : 'border-border hover:border-border-dark'}
    ${leftIcon ? 'pl-10' : ''}
    ${rightIcon ? 'pr-10' : ''}
    ${className}
  `;

  return (
    <div className={containerStyles}>
      {label && (
        <label htmlFor={inputId} className="block text-sm font-medium text-text-primary">
          {label}
          {props.required && <span className="text-error ml-1" aria-hidden="true">*</span>}
        </label>
      )}
      <div className="relative">
        {leftIcon && (
          <span className="absolute left-3 top-1/2 -translate-y-1/2 text-text-muted pointer-events-none">
            {leftIcon}
          </span>
        )}
        <input
          id={inputId}
          className={inputStyles}
          aria-invalid={hasError}
          aria-describedby={hasError ? `${inputId}-error` : hint ? `${inputId}-hint` : undefined}
          {...props}
        />
        {rightIcon && (
          <span className="absolute right-3 top-1/2 -translate-y-1/2 text-text-muted pointer-events-none">
            {rightIcon}
          </span>
        )}
      </div>
      {error && (
        <p id={`${inputId}-error`} className="text-sm text-error" role="alert">
          {error}
        </p>
      )}
      {hint && !error && (
        <p id={`${inputId}-hint`} className="text-sm text-text-tertiary">
          {hint}
        </p>
      )}
    </div>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Input.tsx
git commit -m "feat: enhance Input component with sizes and validation

- Add sm/md/lg sizes
- Add error state with aria-invalid
- Add hint text support
- Add leftIcon/rightIcon props
- Improve accessibility with aria-describedby"
```

---

## Task 4: Enhance Modal Component

**Goal:** Add sizes, improve accessibility, and add close button variants.

**Files:**
- Modify: `src/components/Modal.tsx`

**Step 1: Update Modal component**

Replace `src/components/Modal.tsx`:

```typescript
import type { ReactNode } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { useEffect, useCallback } from 'react';

interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'full';
  children: ReactNode;
  showCloseButton?: boolean;
}

export function Modal({
  isOpen,
  onClose,
  title,
  size = 'md',
  children,
  showCloseButton = true,
}: ModalProps) {
  const handleEscape = useCallback((e: KeyboardEvent) => {
    if (e.key === 'Escape') onClose();
  }, [onClose]);

  useEffect(() => {
    if (isOpen) {
      document.addEventListener('keydown', handleEscape);
      document.body.style.overflow = 'hidden';
    }

    return () => {
      document.removeEventListener('keydown', handleEscape);
      document.body.style.overflow = '';
    };
  }, [isOpen, handleEscape]);

  const sizeStyles = {
    sm: 'max-w-sm',
    md: 'max-w-md',
    lg: 'max-w-lg',
    xl: 'max-w-xl',
    full: 'max-w-[calc(100vw-2rem)] max-h-[calc(100vh-2rem)]',
  };

  return (
    <AnimatePresence>
      {isOpen && (
        <>
          {/* Backdrop */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            onClick={onClose}
            className="fixed inset-0 bg-black/50 backdrop-blur-sm z-modal-backdrop"
            aria-hidden="true"
          />

          {/* Modal */}
          <div
            className="fixed inset-0 z-modal flex items-center justify-center p-4"
            role="dialog"
            aria-modal="true"
            aria-labelledby={title ? 'modal-title' : undefined}
          >
            <motion.div
              initial={{ opacity: 0, scale: 0.95, y: 20 }}
              animate={{ opacity: 1, scale: 1, y: 0 }}
              exit={{ opacity: 0, scale: 0.95, y: 20 }}
              transition={{ type: 'spring', damping: 25, stiffness: 300 }}
              className={`bg-bg-primary rounded-2xl shadow-2xl ${sizeStyles[size]} w-full max-h-[90vh] overflow-hidden flex flex-col`}
            >
              {title && (
                <div className="flex-shrink-0 border-b border-border px-6 py-4 flex justify-between items-center bg-bg-secondary/50">
                  <h2 id="modal-title" className="text-xl font-bold text-text-primary">
                    {title}
                  </h2>
                  {showCloseButton && (
                    <button
                      onClick={onClose}
                      className="p-2 rounded-lg hover:bg-bg-tertiary transition-colors text-text-tertiary hover:text-text-primary focus:outline-none focus-visible:ring-2 focus-visible:ring-primary-500"
                      aria-label="Close modal"
                    >
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="20"
                        height="20"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        aria-hidden="true"
                      >
                        <line x1="18" y1="6" x2="6" y2="18" />
                        <line x1="6" y1="6" x2="18" y2="18" />
                      </svg>
                    </button>
                  )}
                </div>
              )}
              <div className="flex-1 overflow-auto p-6 relative">
                {children}
              </div>
            </motion.div>
          </div>
        </>
      )}
    </AnimatePresence>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Modal.tsx
git commit -m "feat: enhance Modal component with sizes

- Add sm/md/lg/xl/full sizes
- Add showCloseButton prop
- Improve accessibility with proper ARIA
- Use spring animation for smoother transitions
- Fix overflow handling for tall content"
```

---

## Task 5: Create Card Component

**Goal:** Create reusable card component for consistent layouts.

**Files:**
- Create: `src/components/Card.tsx`

**Step 1: Create Card component**

Create `src/components/Card.tsx`:

```typescript
import type { ReactNode, HTMLAttributes } from 'react';

interface CardProps extends HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'elevated' | 'outlined' | 'glass';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  children: ReactNode;
}

export function Card({
  variant = 'default',
  padding = 'md',
  className = '',
  children,
  ...props
}: CardProps) {
  const baseStyles = 'rounded-2xl transition-all';

  const variantStyles = {
    default: 'bg-bg-primary border border-border',
    elevated: 'bg-bg-primary shadow-lg hover:shadow-xl',
    outlined: 'bg-transparent border-2 border-border',
    glass: 'bg-bg-glass backdrop-blur-md border border-bg-glass-border',
  };

  const paddingStyles = {
    none: '',
    sm: 'p-3',
    md: 'p-4',
    lg: 'p-6',
  };

  return (
    <div
      className={`${baseStyles} ${variantStyles[variant]} ${paddingStyles[padding]} ${className}`}
      {...props}
    >
      {children}
    </div>
  );
}

interface CardHeaderProps extends HTMLAttributes<HTMLDivElement> {
  title: string;
  subtitle?: string;
  action?: ReactNode;
}

export function CardHeader({ title, subtitle, action, className = '', ...props }: CardHeaderProps) {
  return (
    <div className={`flex items-start justify-between gap-4 ${className}`} {...props}>
      <div>
        <h3 className="text-lg font-semibold text-text-primary">{title}</h3>
        {subtitle && <p className="text-sm text-text-secondary mt-0.5">{subtitle}</p>}
      </div>
      {action && <div className="flex-shrink-0">{action}</div>}
    </div>
  );
}

export function CardContent({ className = '', children, ...props }: HTMLAttributes<HTMLDivElement>) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  );
}

export function CardFooter({ className = '', children, ...props }: HTMLAttributes<HTMLDivElement>) {
  return (
    <div className={`border-t border-border pt-4 mt-4 ${className}`} {...props}>
      {children}
    </div>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Card.tsx
git commit -m "feat: create Card component with variants

- Add default/elevated/outlined/glass variants
- Add padding options (none/sm/md/lg)
- Add CardHeader, CardContent, CardFooter subcomponents
- Support glass morphism effect"
```

---

## Task 6: Create Badge Component

**Goal:** Create badge/tag component for labels and status indicators.

**Files:**
- Create: `src/components/Badge.tsx`

**Step 1: Create Badge component**

Create `src/components/Badge.tsx`:

```typescript
import type { HTMLAttributes, ReactNode } from 'react';

interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  variant?: 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  children: ReactNode;
}

export function Badge({
  variant = 'default',
  size = 'md',
  className = '',
  children,
  ...props
}: BadgeProps) {
  const baseStyles = 'inline-flex items-center font-medium rounded-full transition-colors';

  const sizeStyles = {
    sm: 'px-2 py-0.5 text-xs',
    md: 'px-2.5 py-1 text-sm',
    lg: 'px-3 py-1.5 text-base',
  };

  const variantStyles = {
    default: 'bg-bg-tertiary text-text-secondary',
    primary: 'bg-primary-100 text-primary-700 dark:bg-primary-900/30 dark:text-primary-400',
    secondary: 'bg-accent-100 text-accent-700 dark:bg-accent-900/30 dark:text-accent-400',
    success: 'bg-success-light text-success dark:bg-success/20',
    warning: 'bg-warning-light text-warning dark:bg-warning/20',
    error: 'bg-error-light text-error dark:bg-error/20',
    outline: 'border border-border text-text-secondary',
  };

  return (
    <span
      className={`${baseStyles} ${sizeStyles[size]} ${variantStyles[variant]} ${className}`}
      {...props}
    >
      {children}
    </span>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Badge.tsx
git commit -m "feat: create Badge component for labels and status

- Add default/primary/secondary/success/warning/error/outline variants
- Add sm/md/lg sizes
- Support dark mode colors"
```

---

## Task 7: Create Avatar Component

**Goal:** Create avatar component for user/partner display.

**Files:**
- Create: `src/components/Avatar.tsx`

**Step 1: Create Avatar component**

Create `src/components/Avatar.tsx`:

```typescript
import type { HTMLAttributes } from 'react';

interface AvatarProps extends HTMLAttributes<HTMLDivElement> {
  src?: string;
  alt?: string;
  fallback?: string;
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
  status?: 'online' | 'offline' | 'busy' | 'away';
}

export function Avatar({
  src,
  alt = 'Avatar',
  fallback,
  size = 'md',
  status,
  className = '',
  ...props
}: AvatarProps) {
  const sizeStyles = {
    xs: 'w-6 h-6 text-xs',
    sm: 'w-8 h-8 text-sm',
    md: 'w-10 h-10 text-base',
    lg: 'w-12 h-12 text-lg',
    xl: 'w-16 h-16 text-xl',
  };

  const statusStyles = {
    online: 'bg-success',
    offline: 'bg-text-muted',
    busy: 'bg-error',
    away: 'bg-warning',
  };

  const statusSizeStyles = {
    xs: 'w-1.5 h-1.5',
    sm: 'w-2 h-2',
    md: 'w-2.5 h-2.5',
    lg: 'w-3 h-3',
    xl: 'w-4 h-4',
  };

  return (
    <div className={`relative inline-block ${className}`} {...props}>
      <div
        className={`${sizeStyles[size]} rounded-full bg-bg-tertiary flex items-center justify-center overflow-hidden`}
      >
        {src ? (
          <img
            src={src}
            alt={alt}
            className="w-full h-full object-cover"
          />
        ) : fallback ? (
          <span className="font-medium text-text-secondary uppercase">
            {fallback.slice(0, 2)}
          </span>
        ) : (
          <svg
            className="w-1/2 h-1/2 text-text-muted"
            fill="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path d="M24 20.993V24H0v-2.996A14.977 14.977 0 0112.004 15c4.904 0 9.26 2.354 11.996 5.993zM16.002 8a4 4 0 11-8 0 4 4 0 018 0z" />
          </svg>
        )}
      </div>
      {status && (
        <span
          className={`absolute bottom-0 right-0 ${statusSizeStyles[size]} ${statusStyles[status]} rounded-full ring-2 ring-bg-primary`}
          aria-label={`Status: ${status}`}
        />
      )}
    </div>
  );
}

interface AvatarGroupProps extends HTMLAttributes<HTMLDivElement> {
  max?: number;
  children: React.ReactNode;
}

export function AvatarGroup({ max = 4, children, className = '', ...props }: AvatarGroupProps) {
  const childArray = Array.isArray(children) ? children : [children];
  const visibleChildren = childArray.slice(0, max);
  const remainingCount = childArray.length - max;

  return (
    <div className={`flex -space-x-2 ${className}`} {...props}>
      {visibleChildren}
      {remainingCount > 0 && (
        <div className="w-10 h-10 rounded-full bg-bg-tertiary flex items-center justify-center text-sm font-medium text-text-secondary ring-2 ring-bg-primary">
          +{remainingCount}
        </div>
      )}
    </div>
  );
}
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/Avatar.tsx
git commit -m "feat: create Avatar component with status indicators

- Add xs/sm/md/lg/xl sizes
- Add online/offline/busy/away status indicators
- Add fallback text support
- Add AvatarGroup for stacked avatars"
```

---

## Task 8: Create Component Index

**Goal:** Create centralized export for all UI components.

**Files:**
- Create: `src/components/index.ts`

**Step 1: Create component index**

Create `src/components/index.ts`:

```typescript
// UI Components
export { Button } from './Button';
export { Input } from './Input';
export { Modal } from './Modal';
export { Card, CardHeader, CardContent, CardFooter } from './Card';
export { Badge } from './Badge';
export { Avatar, AvatarGroup } from './Avatar';
export { Loader } from './Loader';
export { LoadingState } from './LoadingState';
export { ErrorBoundary } from './ErrorBoundary';

// Layout Components
export { AnimatedBackground } from './layout/AnimatedBackground';

// Feature Components
export { BottomNav } from './BottomNav';
export { Confetti } from './Confetti';
export { InvitePartner } from './InvitePartner';
export { JoinPartner } from './JoinPartner';
export { Modal } from './Modal';
export { ProtectedRoute } from './ProtectedRoute';
export { ShareCard } from './ShareCard';
export { ThemeProvider } from './ThemeProvider';
```

**Step 2: Verify build**

Run:
```bash
npm run typecheck
npm run build
```

**Step 3: Commit**

```bash
git add src/components/index.ts
git commit -m "feat: create component index for centralized exports

- Export all UI components from single entry point
- Organize by category (UI, Layout, Feature)"
```

---

## Task 9: Final Verification

**Goal:** Ensure all components work correctly.

**Step 1: Run full test suite**

Run:
```bash
npm test
```

**Step 2: Run lint**

Run:
```bash
npm run lint
```

**Step 3: Run type check**

Run:
```bash
npm run typecheck
```

**Step 4: Run production build**

Run:
```bash
npm run build
```

**Step 5: Check bundle size**

Run:
```bash
ls -lh dist/assets/*.js | head -10
```

**Step 6: Final commit**

```bash
git add -A
git commit -m "chore: Phase 3 UI/UX overhaul complete

- Enhanced Button with sizes, variants, loading state
- Enhanced Input with sizes, error states, icons
- Enhanced Modal with sizes, better accessibility
- Created Card, Badge, Avatar components
- Created component index for centralized exports
- All components use design tokens via Tailwind"
```

---

## Verification Checklist

After completing all tasks, verify:

- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `npm run lint` shows no new errors
- [ ] `npm run typecheck` passes
- [ ] Components render correctly
- [ ] Dark mode works
- [ ] Accessibility: focus states visible
- [ ] Touch targets are 44px minimum

---

## Rollback Plan

If critical issues arise:

1. **Component issues**: Revert specific component
   ```bash
   git revert HEAD~1  # Revert last commit
   ```

2. **Tailwind config issues**: Restore previous config
   ```bash
   git checkout HEAD~5 -- src/styles/tailwind.css
   ```

---

## Next Steps After Phase 3

Once Phase 3 is complete and verified:

1. **Phase 4**: Minigame expansion (achievements, new games)
2. **Phase 5**: Advanced PWA (background sync, push notifications)

---

**Plan saved to:** `docs/plans/2026-03-13-phase3-ui-ux.md`
